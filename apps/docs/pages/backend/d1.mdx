import { Callout } from 'nextra/components'
import { Steps } from 'nextra/components'

# 🔌 Backend

## Create a database

<Callout type="info">
  For more info, see [the official Cloudflare D1
  docs](https://developers.cloudflare.com/d1/get-started/#2-create-a-database).
</Callout>

Run this command three times to create two databases.

<Steps>
### Step 1
 
Create a preview database.
 
### Step 2
 
Create a production database.
</Steps>

```bash
wrangler d1 create <DATABASE_NAME>
```

The output will look like this:

```bash
✅ Successfully created DB '<DATABASE_NAME>'

[[d1_databases]]
binding = "DB"
database_name = "<DATABASE_NAME>"
database_id = "<unique-ID-for-your-database>"
```

